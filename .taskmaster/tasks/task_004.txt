# Task ID: 4
# Title: Scaffold Node.js Notion Consumer service
# Status: pending
# Dependencies: [1]
# Priority: medium
# Description: Create standalone Node.js application that consumes workout logs from Kafka with proper consumer group configuration

# Details:
Initialize Node.js project 'notion-consumer' with package.json including dependencies: kafkajs, @notionhq/client, dotenv, winston for logging. Create main consumer application with consumer group 'notion-consumer-group' consuming from 'workout-logs' topic. Implement message deserialization and basic validation. Configure auto-commit with proper offset management. Add graceful shutdown handling for SIGINT/SIGTERM with proper consumer disconnection. Set up structured logging with winston including timestamp and log levels.

# Test strategy:
Test consumer connects to Kafka successfully and joins consumer group, verify it receives messages and processes them without errors, test graceful shutdown scenarios, validate logging output is structured and informative 
